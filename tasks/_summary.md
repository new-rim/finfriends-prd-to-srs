# 이슈 한 줄 요약 — Stage 1~9 (전체)

**대상:** `tasks/stage-1/` ~ `tasks/stage-9/` · **46건 (전체)**
**기준 문서:** `TASKS_finfriends-nextjs-v3_0.md` (개정 3.0 · 55건)
**작성 순서:** `REPORT_task-extraction-review.md` (개정 3.0) §4 — 9 Stage
**짝 문서:** `tasks/_index.md` (Stage 진행 현황 · 이슈번호 매핑 · Stage별 상세 요약)

> **이 문서의 쓰임** — 46개 이슈를 **한 화면에서 훑기 위한 것**이다. 각 줄은 제목이 아니라 **그 이슈의 핵심 제약 한 가지**를 담는다.
> ⚠️ **의사결정 근거로 쓰기엔 얇다.** GWT·DoD·차단 항목은 `tasks/stage-N/` 원문에 있고, 실제 착수 판단은 거기서 한다.

---

## Stage 1 — 플랫폼·데이터 명세 (4건 · 흡수 17 · AC 6)

| ID | 한 줄 |
| --- | --- |
| `FR-001` | 배포 단위 1개를 세우고 리전을 고정하며 **Preview가 운영 아동 데이터에 닿지 못하게** 환경 스코프를 분리한다 — 유일한 진입점 |
| `FR-002` | `app`·`pii` 다중 스키마로 모델 전수를 확정한다 — **이후 45개 이슈의 References·DoD가 여기서 정한 이름을 가리킨다** |
| `FR-003` | 2클라이언트·RLS 2계층·`pii` 권한 회수로 **애플리케이션이 실수해도 DB가 남의 아동 데이터를 주지 않는 상태**를 만든다 |
| `FR-004` | advisory lock + 단일 SQL + 유니크 키로 **동시 지급에도 `balance_after`가 어긋나지 않게** 한다 — 별이 걸린 전 기능의 바닥 |

## Stage 2 — 계약·Mock (5건 · 흡수 14 · AC 4)

| ID | 한 줄 |
| --- | --- |
| `FR-006` | 모든 Server Action이 통과할 **5단계 골격**(검증→동의 재확인→원자 기입→계측→선택 재검증)과 멱등 키·캐시 태그를 만든다 |
| `FR-007` | 나무 승급·정체·계획 대조·WPA를 **DB·React 비의존 순수 함수**로 분리한다 — 배치와 화면이 같은 답을 내야 하므로 |
| `FR-008` | Server Action 20종을 6개 도메인 계약으로 묶어 **입력 스키마·반환 타입·실패 분기**를 못 박는다 — 프론트가 타입만 보고 착수 가능 |
| `FR-009` | 웹훅 픽스처·화면 시드·배치 스텁 3종 — **완료 조건이 아니라 착수 해제 수단**(D-TEC-2 미해소여도 진행) |
| `FR-024` | 제휴사가 우리를 부르는 유일한 형태를 확정한다 — HMAC 검증·5분 창·**처리 실패에도 200**(5xx는 커넥션을 고갈시킨다) |

## Stage 3 — 강제 수단·기반 설비 (5건 · 흡수 19 · AC 12)

| ID | 한 줄 |
| --- | --- |
| `FR-005` | 게이트 러너 + 7종 검사 — **빌드가 유일한 강제 지점**이므로 `prebuild`·`build` 양쪽에서 부르고 실행 흔적을 로그에 남긴다 |
| `FR-010` | 보호자만 Auth 사용자로 두고 아동은 서명 쿠키로만 표현한다 — **아동 자격증명이 없으면 유출될 것도 없다** |
| `FR-011` | `(child)/layout.tsx`의 `noStore()` 확정 판정 + Action 래퍼 재확인 — 동의 4겹 중 **읽기·쓰기 두 겹** |
| `FR-030` | 하트비트·`pg_net` 진입점·등록 목록 대조 — 배치가 두 곳으로 나뉘어 **한쪽이 조용히 멈춘 상태**가 가장 흔한 장애다 |
| `FR-035` | `CRON_SECRET` 공통 인증 위에 가용성 프로브와 원가 집계를 올린다 — **외부에서 두드려야 의미가 있는** 배치들 |

## Stage 4 — 상태 변경 (5건 · 흡수 13 · AC 9)

| ID | 한 줄 |
| --- | --- |
| `FR-012` | 온보딩 3액션 — 매 단계 DB 커밋 · 동의 **버전** 기록 · 카드 신청 실패 시 **입력값 24h 보존**(재입력 0건) |
| `FR-014` | 별을 지급하는 **유일한 내부 경로** — 트리거 8종 · 학습/실천 분리 · **차감도 같은 함수**(부호만 다름) |
| `FR-015` | 원고 DB 적재(런타임 변경 가능)와 이수·퀴즈 기록 — **승급 판정은 하지 않고 `FR-007`에 위임** |
| `FR-017` | 미션 생성→완료→승인/거절/일괄 — **승인·⭐·실천 인정·이벤트 4건이 한 트랜잭션**이고 소급은 `earned_at` 기준 |
| `FR-020` | 위시리스트 단계 보상과 아바타 교환 — 중복은 **DB 제약**으로 막고 **차감도 같은 원장 경로**를 탄다 |

## Stage 5 — 소비·회고·배치·알림·오프라인 (8건 · 흡수 24 · AC 31)

| ID | 한 줄 |
| --- | --- |
| `FR-022` | 어디서·업종·얼마까지를 저장하되 업종을 **카드 승인 코드와 대조 가능한 값**으로 — 작성률 50%가 생존 조건 |
| `FR-025` | 웹훅 수신·DLQ·카드 상태·해지 + **결제↔계획 매칭을 수신 트랜잭션 안에서 즉시**(배치로 미루면 대조할 것이 없다) |
| `FR-027` | 문장 비복원 배정과 두 갈래 판정 — **갈래 B는 ⭐를 안 줄 뿐 차감하지 않는다**(벌이 아니라 되돌아보기) |
| `FR-032` | `pg_cron`이 전 아동을 순회해 `stall_days`를 산출한다 — **화면이 재계산하면 값이 갈라져 검증 기준이 사라진다** |
| `FR-033` | WPA·주간 지표·월간 숲·업종별 집계 — 전부 대량이라 `pg_cron`. **규모가 커진 뒤에는 못 옮긴다** |
| `FR-034` | 푸시→배너→SMS 3단 폴백 — 판정 기준은 발송률이 아니라 **「전달 시도 100%」**, 근거는 채널별 분리 집계 |
| `FR-036` | 72시간 미접속 판정·발송과 DLQ 재처리 — **71시간에 돌아온 아이에게 보내면 뒤의 진짜 알림도 안 열린다** |
| `FR-039` | PWA 셸 + IndexedDB 큐 + iOS 포그라운드 flush — **손실·중복·`client_ts` 귀속은 신뢰의 문제, 반영 시점은 체감의 문제** |

## Stage 6 — 조회 (3건 · 흡수 3 · AC 5)

> 화면(Stage 7)보다 먼저 쓴다 — 반환 형태가 확정돼야 화면이 인용할 수 있다. **3건 전부 병렬 착수 가능.**

| ID | 한 줄 |
| --- | --- |
| `FR-018` | 「승인 대기 N건」을 **한 지점에서만 세고** 대기·거절·미실천 3종과 일괄 후보를 구별해 반환한다 — 짝인 화면은 AC 0건 |
| `FR-028` | 미완 회고를 순서대로 주되 **3건 초과 시 요약으로 병합**한다 — 밀린 걸 다 시키면 아동이 회고 자체를 피한다 |
| `FR-037` | 병렬 4쿼리로 배치 산출값을 **읽기만** 한다 — **p95 예산 180ms 칸을 단독으로 져서** 조회 3건 중 유일하게 분리 유지 |

## Stage 7 — 화면 (8건 · 흡수 14 · AC 18)

> **8건 전부 병렬.** 내부 선행이 없고 전부 말단 이슈다 — 대신 **UX 레인 9건이 실질 선행**이다.

| ID | 한 줄 |
| --- | --- |
| `FR-013` | 온보딩 Stepper와 `(public)` 화면 — **재입력 0건은 `FR-012`가 저장하고 이 이슈가 보여 줘야** 성립한다 |
| `FR-016` | 학습·퀴즈와 아동 온보딩 — **첫 보상 루프 5분 이내** · 「불리기 곧 열려요」로 결함 오인을 막는다 |
| `FR-019` | 승인 대기 Table·Dialog·Checkbox — **대기·거절·미실천을 색+문구 양쪽으로** 가른다(AC는 `FR-018` 귀속) |
| `FR-021` | 위시리스트 단계 표기와 아바타 옷장 — **별 잔액은 `noStore`**, 방금 받은 별이 안 보이면 루프가 끊긴다 |
| `FR-023` | 계획 카드 Form — **업종은 Select만**(자유 텍스트면 매칭 90% 불가) · 항목 3개를 늘리면 작성률 50%가 무너진다 |
| `FR-026` | `PARTNER_CARDS.state`로 **컴포넌트 단위 잠금** — 라우트를 막으면 아동이 아무것도 못 한다 |
| `FR-029` | 두 갈래 회고 — **갈래 B에 경고색을 쓰지 않는다**(벌이 아니다) · 임계 경로 10단계의 **종점** |
| `FR-038` | 나무·정체 원인·월간 숲·소비 4화면 — **AC 12건**(상한 초과) 중 **6건이 인터뷰 rubric**이라 코드 리뷰로 못 통과시킨다 |

## Stage 8 — 비기능·관측·조건부 AI (5건 · 흡수 16 · AC 13)

| ID | 한 줄 |
| --- | --- |
| `FR-031` | `pg_cron` 점검 5종(파티션·원장 정산·권한 스캔·계측 건강성·문장 풀) — **기능이 아니라 감시**, 멈추면 아무도 모른다 |
| `FR-040` | 로그 외부 드레인 · 규제 이벤트 알림 · 배치 침묵 감지 — **SLA는 발송이 아니라 30분 내 「확인」까지**다 |
| `FR-041` | §8 예산을 구간별로 재고 초과 시 **어느 칸인지와 함께 보고** — 없으면 §8.4가 1번 조치부터 막힌다 |
| `FR-042` | 운영자 전용 AI — 프롬프트 허용 입력 **4종뿐** · 승인 전 배정 0 · **`AI_ENABLED=false`여도 요구사항 손실 0** |
| `FR-043` | AI 초안 검토·문장 풀 화면 — **AI를 꺼도 문장 풀 관리는 남는다**(사람이 쓴 문장도 잔여율 감시 대상) |

## Stage 9 — 검증 설비 (3건 · 흡수 10 · AC 35)

> **AC 35건이 3건에 몰려 있다** — 전체 84건의 42%. 상한 초과 3건 중 2건이 여기다.

| ID | 한 줄 |
| --- | --- |
| `FR-044` | 게이트 7종에 위반을 주입해 **7/7 빌드 실패**를 확인 — **RG-T1의 단독 근거**, 로컬 빌드에서도 같아야 한다 |
| `FR-045` | RLS·부하·배치·배포 감사 — **RG-T2·RG-T3 두 게이트**를 지며 선행 8건으로 사실상 전체가 서야 돌린다 |
| `FR-046` | 동의·오프라인 2 OS·웹훅·폴백·AI 경계 E2E — **AC 15건(최대)**, 하네스는 여기서 만들되 판정은 Feature와 공유 |

---

## 완료 현황

| 항목 | 값 |
| --- | :-: |
| 작성 완료 | ✅ **46 / 46건** |
| 개정 2.0 태스크 흡수 | ✅ **130 / 130건** |
| AC 귀속 *(유니크)* | ✅ **84 / 84건** |
| 검증 결함 | ✅ **0건** |

**9 Stage 전체 작성이 끝났다.** 다음은 `REPORT_task-extraction-review.md` §4의 **Stage 10(오케스트레이션)** — 이슈 생성과 `FR-###` → `#번호` 치환이다.

**이슈 생성 전 처리할 것 2가지** — ① `References` 링크가 46파일 전부 `(#)` 플레이스홀더다 ② **AC 상한 초과 3건**(`FR-046` 15 · `FR-038` 12 · `FR-045` 12)의 분리 여부. 전부 분리하면 **+3건(49건)**.

> **AC 귀속 수치 정정** — 직전 판본은 Stage 1~5를 **62건**으로 적었으나, 이는 **여러 이슈에 걸친 AC를 중복으로 센 값**이다. 유니크 기준으로는 **58건**이었고, Stage 6이 3건(`AC-6.2` · `AC-6.3` · `ACE-5.2`)을 더해 **61건**이 되었다.

---

*근거: `tasks/stage-1/` ~ `tasks/stage-9/` 46개 파일 · `TASKS_finfriends-nextjs-v3_0.md` §3 · §8*
