# [방법론] SRS 기반 개발 태스크 추출 4단계

**문서 ID:** METHODOLOGY-TASK-EXTRACTION-001
**개정 버전:** 1.0
**성격:** 프로젝트 비종속 **표준 방법론** — 특정 스택·도메인에 매이지 않는다
**적용 대상:** SRS(또는 이에 준하는 요구사항 명세)에서 GitHub Project·이슈 트래커에 올릴 개발 태스크를 도출하는 작업
**짝 문서:** `.github/ISSUE_TEMPLATE/feature-task.md` (이슈 본문 템플릿)

---

## 0. 개요

### 0.1 이 방법론이 푸는 문제

요구사항 문서를 기능 목록으로만 쪼개면 **모두가 서로를 기다리는 태스크 더미**가 나온다. 프론트는 백엔드를, 백엔드는 스키마를, 테스트는 구현을 기다린다.

이 방법론은 순서를 뒤집는다 — **먼저 계약을 고정하고, 그 위에서 로직을 나누고, 완료 조건을 실행 가능한 테스트로 바꾼 뒤, 마지막에 선후 관계를 묶는다.**

### 0.2 4단계 한눈에

| Step | 이름 | 스캐닝 대상 (SRS의 어디) | 산출 태스크 유형 |
| :-: | --- | --- | --- |
| **1** | 계약·규약 및 데이터 명세 | 데이터 모델 · 인터페이스/API 명세 | `[DB]` · `[API Spec]` · `[Mock]` |
| **2** | 로직 및 상태 변경 분해 (CQRS) | 기능적 요구사항 | `[Feature/Query]` · `[Feature/Command]` |
| **3** | 완료 조건(AC) → 테스트 변환 | 인수 조건(AC) | `[Test]` (+ Feature의 DoD 삽입) |
| **4** | 비기능 제약 추출 및 의존성 매핑 | 비기능 요구사항(보안·성능·운영) | `[Infra]` · `[Sec]` + `Blocks`/`Depends on` |

### 0.3 관통하는 원칙 3가지

1. **계약이 먼저다** — Step 1의 산출물은 기능이 아니라 **기준점**이다. 이것이 있어야 백엔드와 프론트엔드가 병렬로 움직인다.
2. **완료 조건은 설명이 아니라 실행 대상이다** — AC를 문장으로 두면 아무도 검증하지 않는다. Step 3에서 **테스트 코드 작성 Task**로 바꾼다.
3. **의존성은 마지막에 한 번에 묶는다** — 태스크를 뽑는 도중에 선후 관계를 정하면 누락이 생긴다. 전수를 뽑아 놓고 Step 4에서 오케스트레이션한다.

---

## 1. Step 1 — 계약 및 규약(Contract or Protocol) 및 데이터(Data) 명세 Task 추출

> **[행동 지침]**
> SRS 문서의 **'데이터 모델(Data Model)'** 및 **'인터페이스/API 명세(Interfaces)'** 섹션을 스캐닝하여,
> 백엔드와 프론트엔드의 **기준점이 될 구조화 태스크를 먼저** 뽑아냅니다.

### 추출 대상 1 — 데이터베이스 Task

- SRS의 **ERD나 데이터 구조 명세** 확인
- 📝 **생성할 이슈 예시**
  ```
  [DB] User 및 Profile 테이블 스키마, 마이그레이션 스크립트 작성
  ```

### 추출 대상 2 — API 및 통신 계약 Task

- SRS의 **시스템 인터페이스 명세(System Context)** 확인
- 📝 **생성할 이슈 예시**
  ```
  [API Spec] Auth 도메인 (회원가입/로그인) Request/Response DTO 및 에러 코드 정의
  ```

### 추출 대상 3 — Mock 데이터 Task

- 프론트엔드 에이전트가 **백엔드 완성을 기다리지 않도록** 가짜 데이터 규격 확인
- 📝 **생성할 이슈 예시**
  ```
  [Mock] 프론트엔드 UI 개발용 회원가입 성공/실패 Mocking API 엔드포인트 세팅
  ```

> **Step 1 완료 판정** — 인터페이스 명세의 **모든 경계**(엔드포인트 또는 서버 함수)가 계약 태스크에 1:1로 대응되고, 그중 프론트가 참조할 것에 Mock이 붙어 있다.

---

## 2. Step 2 — 로직(Logic) 및 상태 변경(Mutation) Task 분해

> **[행동 지침]**
> SRS 문서의 **'기능적 요구사항(Functional Requirements)'** 을 스캐닝하여,
> Step 1에서 만든 데이터가 **어떻게 조회되고 변경되는지** 분해하여 이슈를 생성합니다. **(CQRS 패턴 적용)**

### 추출 대상 1 — Read (Query) Task

- 데이터를 **단순히 읽어와서 보여주는** 요구사항 분리
- 📝 **생성할 이슈 예시**
  ```
  [Feature/Query] 마이페이지 유저 프로필 및 활동 내역 조회 로직 구현
  ```

### 추출 대상 2 — Write (Command) Task

- 데이터베이스의 **상태를 변경하는 핵심 비즈니스 로직** 분리 (입력 검증 포함)
- 📝 **생성할 이슈 예시**
  ```
  [Feature/Command] 이메일 중복 검증 및 비밀번호 암호화 기반 회원가입 비즈니스 로직 구현
  ```

> **Step 2 완료 판정** — 화면 하나에 Query와 Command가 섞여 한 이슈에 들어간 것이 없다. **담당자·검증 방식·완료 판정이 다르면 다른 이슈다.**

---

## 3. Step 3 — 완료 조건(AC)을 테스트(Test) Task로 변환 추출

> **[행동 지침]**
> SRS 각 기능에 정의된 **'인수 조건(Acceptance Criteria)'** 을 단순한 설명이 아닌,
> 에이전트가 실행해야 할 **'테스트 코드 작성 Task'** 로 변환하여 추출합니다.

### 추출 대상 1 — TDD 기반 테스트 시나리오 Task

- 요구사항의 **성공/실패 시나리오** 확인 (GWT 포맷)
- 📝 **생성할 이슈 예시**
  ```
  [Test] 회원가입 GWT 시나리오 기반의 단위 테스트 및 예외 처리(409 Conflict 등) 테스트 코드 작성
  ```

> 💡 **Tip** — 이 과정에서 추출된 구체적인 AC 내용들은 **Step 2에서 생성한 Feature 이슈의 `Definition of Done (DoD)` 체크리스트 항목으로 바로 삽입**됩니다.

> **Step 3 완료 판정** — SRS의 AC 전건이 **① 어느 Feature 이슈의 DoD에 들어갔거나 ② `[Test]` 이슈로 변환**되었다. 어느 쪽에도 없는 AC가 0건이다.

---

## 4. Step 4 — 비기능 제약(NFR) Task 추출 및 의존성(Dependency) 매핑

> **[행동 지침]**
> SRS 문서의 **'비기능적 요구사항(보안, 성능, 운영)'** 을 인프라 및 DevOps Task로 추출하고,
> 지금까지 뽑아낸 **전체 이슈들을 스프레드시트나 GitHub Project에 올려 선후 관계를 묶습니다.**

### 추출 대상 1 — 인프라 및 보안/성능 Task

- **응답 속도 제한, 로깅 정책, 권한 체계** 등 파악
- 📝 **생성할 이슈 예시**
  ```
  [Infra] Auth API 응답시간 p95 ≤ 300ms 검증을 위한 k6 부하 테스트 스크립트 작성
  ```
  ```
  [Sec] 사용자 비밀번호 단방향 암호화(Bcrypt) 및 로깅 마스킹 파이프라인 구축
  ```

### 추출 대상 2 — 의존성(Dependency) 설정 *(핵심 오케스트레이션)*

- 태스크 추출 완료 후, 이슈 간의 **`Blocks` / `Depends on`** 관계를 명시
- *예: `[Feature/Command]` 회원가입 로직은 `[DB]` User 테이블 마이그레이션 이슈가 끝나야만 시작할 수 있음.*

> **Step 4 완료 판정** — 모든 이슈가 **선행이 자기보다 먼저 끝나는 순서**로 정렬되고, 순환 의존이 0건이다. 진입점(선행 없음) 이슈가 명확하다.

---

## 5. 이슈 접두사 체계

본문의 예시에서 도출한 유형 목록이다. **접두사를 고정하면 GitHub Project에서 유형 필터가 성립한다.**

| 접두사 | Step | 무엇을 만드나 | 주 담당 |
| --- | :-: | --- | --- |
| `[DB]` | 1 | 스키마 · 마이그레이션 · 인덱스 · 접근 정책 | 백엔드 |
| `[API Spec]` | 1 | 요청·응답 계약 · 에러 코드 · 버전 규약 | 백엔드 |
| `[Mock]` | 1 | 픽스처 · 스텁 엔드포인트 · 시드 규격 | 백엔드 또는 프론트 |
| `[Feature/Query]` | 2 | 조회 로직 | 백엔드 |
| `[Feature/Command]` | 2 | 상태 변경 로직 + 입력 검증 | 백엔드 |
| `[Test]` | 3 | GWT 기반 테스트 코드 | 백엔드 또는 QA |
| `[Infra]` | 4 | 성능·운영·부하 검증 설비 | DevOps |
| `[Sec]` | 4 | 보안·권한·마스킹 파이프라인 | 백엔드 · 보안 |

> 화면 구현을 별도 유형으로 관리한다면 `[UI]`를 추가한다 — Step 2의 Query와 짝이 되어 **조회 로직과 렌더를 병렬화**할 수 있다.

---

## 6. 적용 시 조정 지침 — 그대로 따르면 어긋나는 경우

방법론은 **일반형**이므로, 프로젝트의 구조가 전제와 다르면 **형태를 옮겨야 한다.** 아래는 실제 적용에서 확인된 조정 지점이다.

| # | 전제 | 어긋나는 경우 | 조정 |
| :-: | --- | --- | --- |
| **1** | Step 1이 REST API 계약(DTO · HTTP 에러 코드)을 산출한다 | **공개 REST API가 없는 스택** (예: 서버 함수·RPC·GraphQL 단일 엔드포인트) | 계약 태스크는 **유지**하되 산출물을 그 스택의 계약 형태로 바꾼다 — 입력 스키마 + 반환 타입 + 실패 분기. HTTP 상태 코드 규약은 **외부 진입점에만** 적용 |
| **2** | Step 4가 NFR을 **인프라 태스크로 분리**한다 | 명세가 *"비기능을 별도 배분하지 말 것"* 을 규정한 경우 *(분리하면 규제·정합성이 일정에서 뒤로 밀리기 때문)* | **규제·정합성 NFR**은 Feature의 DoD로 흡수하고, **성능·운영 NFR만** `[Infra]`·`[Sec]`로 분리한다. NFR을 잊지 않는다는 취지는 DoD로도 달성된다 |
| **3** | Step 2가 Query와 Command를 항상 나눈다 | 조회가 얕거나 게이트·셸 성격인 화면 | **나누지 않는다.** 태스크 수만 늘고 병렬화 이득이 없다. 분할 기준은 *"담당자와 검증 방식이 다른가"* 이지 *"조회가 있는가"* 가 아니다 |
| **4** | Step 3의 테스트가 기능마다 붙는다 | 위반 주입·부하 하네스처럼 **개별 기능에 귀속되지 않는 검증 설비** | 이런 것은 Step 3이 아니라 **Step 4의 `[Infra]`** 로 분류한다. 기능별 GWT만 DoD로 흡수한다 |

> **조정할 때의 원칙** — 방법론의 **형식**이 아니라 **취지**를 지킨다. Step 1의 취지는 *"병렬 착수의 기준점 확보"* 이고, Step 4의 취지는 *"비기능을 잊지 않기"* 다. 취지가 달성되면 산출물의 형태는 스택을 따라가도 된다.

---

## 7. 실행 체크리스트

```
[ ] Step 1  데이터 모델 · 인터페이스 명세 스캔
    [ ] [DB]        스키마·마이그레이션 태스크
    [ ] [API Spec]  계약 태스크 — 경계 전수에 1:1 대응하는가
    [ ] [Mock]      프론트가 참조할 경계에 픽스처가 있는가
[ ] Step 2  기능 요구사항 스캔 (CQRS)
    [ ] [Feature/Query]    조회 로직
    [ ] [Feature/Command]  상태 변경 로직 + 입력 검증
    [ ] 한 이슈에 Query와 Command가 섞이지 않았는가
[ ] Step 3  AC 스캔 → 테스트 변환
    [ ] [Test]  GWT 시나리오 기반 테스트 코드 태스크
    [ ] AC 전건이 DoD 또는 [Test]에 귀속되었는가 (미귀속 0건)
[ ] Step 4  NFR 스캔 + 오케스트레이션
    [ ] [Infra] 성능·운영 검증 설비
    [ ] [Sec]   보안·권한·마스킹
    [ ] 전 이슈의 Blocks / Depends on 명시
    [ ] 순환 의존 0건 · 진입점 명확
```

---

## 8. 적용 이력

| 프로젝트 | 대상 명세 | 적용 결과 |
| --- | --- | --- |
| FinFriends (Next.js 판본) | `SRS_finfriends-nextjs-v1_0.md` | 대조 평가 보고서 — `REPORT_task-extraction-review.md`. §6의 조정 지침 1·2·3·4가 이 적용에서 도출됨 |

---

*본 방법론의 §1~§4 본문은 제시된 원문을 보존한 것이며, §0 · §5~§7은 문서화 과정에서 원문에서 도출한 보조 절이다.*
