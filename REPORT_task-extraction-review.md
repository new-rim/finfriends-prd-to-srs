# [평가 보고서] 태스크 추출 방법론 대조 — 현재 태스크 리스트의 수정 여지와 풀버전 작성 순서

**문서 ID:** REPORT-TASK-EXTRACTION-001
**평가 대상:** `TASKS_finfriends-nextjs-v1_0.md` (139건 · FR 116 · UX 23)
**평가 기준:** 사용자 제시 4단계 추출 방법론 (계약·데이터 → 로직 CQRS → AC·테스트 → NFR·의존성)
**근거 문서:** `SRS_finfriends-nextjs-v1_0.md` · `SRS_finfriends-v1_0.md`

---

## 0. 한 줄 결론

현재 문서는 **Step 4(의존성 매핑)에서 가장 강하고, Step 1(계약)과 Step 2(CQRS 분리)에서 가장 약하다.**
원인은 하나다 — 이 문서는 **「무엇을 언제 만드는가(빌드 순서)」로 최적화**되어 있고, 방법론이 요구하는 **「누가 무엇을 병렬로 만들 수 있는가(계약 기준점)」로는 최적화되어 있지 않다.**

| Step | 방법론이 요구하는 것 | 현재 상태 | 판정 |
| :-: | --- | --- | :-: |
| **1** | 계약·데이터 태스크를 **먼저** 뽑아 프론트·백의 기준점 확보 | DB는 충실(E2 10건). **입출력 계약이 공통 골격 1건에 뭉쳐 있고, Mock은 0건** | **△** |
| **2** | Read(Query) / Write(Command) 분해 | Command는 잘 분리(Server Action 20종 전수). **Query가 UI와 한 태스크에 묶여 16건** | **△** |
| **3** | AC를 테스트 태스크로 변환 + Feature의 DoD로 삽입 | 테스트가 **E20에 10건으로 몰려 있고**, AC 84건 중 태스크에 인용된 것 **29건(34%)** | **✕** |
| **4** | NFR 추출 + `Blocks`/`Depends on` 매핑 | 의존성 **114/116 보유 · Blocks 역산 가능**. 단 **성능 예산 계측 태스크 부재** | **○** |

---

## 1. Step 1 — 계약 및 데이터 명세

### 1.1 잘 되어 있는 것

- **데이터베이스 태스크** — E2(FR-008~017) 10건이 Prisma 스키마 · 롤 분리 · RLS · 원자 기입 · 파티셔닝까지 분해되어 있다. 방법론의 「추출 대상 1」을 충족한다.
- **인터페이스 식별** — SRS §6.1.1의 Server Action **20종 전수**, §6.1.2 Route Handler **9종 전수**가 태스크에 1:1로 등장한다. 인터페이스 **목록**은 빠짐이 없다.

### 1.2 수정 여지 ① — 입출력 계약이 태스크로 분리되어 있지 않다 🔴

방법론은 *"[API Spec] Auth 도메인 Request/Response DTO 및 에러 코드 정의"* 를 **독립 태스크**로 요구한다. 현재 문서에서 이에 해당하는 태스크는 **FR-026 「Server Action 공통 골격」 1건뿐**이고, 개별 액션 20종의 **입력 스키마·반환 형태·실패 코드**는 각 기능 태스크 안에 묻혀 있다.

| 결과 |
| --- |
| 프론트엔드가 참조할 **계약 기준점이 존재하지 않는다** — 화면 태스크 착수 시 서버 태스크 완료를 기다려야 한다 |
| `zod` 언급 태스크가 **3건뿐**(FR-008 · FR-022 · FR-026)이며, 그중 계약을 *정의*하는 것은 FR-026 하나다 |
| 템플릿의 `References > 서버 경계` 슬롯을 채울 **1차 산출물이 없다** |

**제안** — E4에 계약 정의 태스크를 **도메인 단위로 신설**한다. 액션 20종을 도메인별로 묶으면 6건이 나온다.

| 신설(안) | 포함 액션 | 근거 |
| --- | --- | --- |
| `[Contract] 온보딩·동의` | `saveOnboardingStep` · `submitConsent` · `requestPartnerCard` · `selectChildProfile` | §6.1.1 · §6.4 |
| `[Contract] 미션·승인` | `createMission` · `reportMissionDone` · `approveMission` · `rejectMission` · `bulkApproveMissions` | §6.1.1 |
| `[Contract] 학습·보상` | `completeLearningTopic` · `submitQuizAnswer` · `redeemAvatarItem` · `updateWishlistSaving` | §6.1.1 |
| `[Contract] 소비·회고` | `createPlanCard` · `submitRetrospective` · `terminatePartnerCard` | §6.1.1 |
| `[Contract] 알림·오프라인` | `registerPushSubscription` · `updateNotifyWindow` · `flushOfflineQueue` | §6.1.1 · REQ-TEC-008 |
| `[Contract] 외부 진입` | Route Handler 9종의 요청·응답·인증 규약 (HMAC · `CRON_SECRET` · 200 응답 규약) | §6.1.2 · §3.4 |

> **주의** — 이 프로젝트에는 **공개 REST API가 없다**(SRS §6.1). 따라서 산출물은 OpenAPI 문서가 아니라 **`zod` 스키마 모듈 + 반환 타입 + 실패 분기 표**다. 방법론의 「DTO 및 에러 코드」를 이 스택의 형태로 옮긴 것이다.

### 1.3 수정 여지 ② — Mock 태스크가 0건이다 🔴

방법론의 「추출 대상 3」이 **전혀 반영되어 있지 않다.** 유사 항목은 FR-006의 *"합성 시드"* 뿐인데, 이것은 **로컬 DB 시드**이지 프론트엔드가 백엔드 없이 화면을 만들기 위한 Mock이 아니다.

이 프로젝트에서 Mock이 특히 중요한 이유가 두 가지 있다.

1. **B4가 외부 계약에 걸려 있다** — 제휴사 API(D1 · D-TEC-2)가 확정되지 않으면 FR-057~063이 착수되지 않는다. 웹훅 페이로드 Mock이 있으면 **매칭 로직(FR-059)까지 선행**할 수 있다.
2. **화면과 배치가 분리되어 있다** — 나무·숲·소비 화면(FR-090·092·093)은 `pg_cron` 산출값을 **읽기만** 한다. 배치 결과 Mock이 있으면 화면을 **배치 완성 전에** 만들 수 있다.

**제안** — 신설 3건.

| 신설(안) | 무엇을 만드나 | 이 태스크가 풀어 주는 것 |
| --- | --- | --- |
| `[Mock] Server Action 응답 모킹 세트` | 액션 20종의 성공·실패 반환 픽스처 | 화면 태스크 16건이 서버를 기다리지 않음 |
| `[Mock] 제휴사 웹훅 페이로드 · 스텁 서버` | 결제 승인 · 카드 상태 서명 페이로드 | **D-TEC-2 지연 시 B4 선행 가능** |
| `[Mock] 배치 산출값 픽스처` | `tree_states` · `forest_snapshots` · `monthly_category_agg` | 전달 화면 4건을 B5 배치와 병렬화 |

---

## 2. Step 2 — 로직 및 상태 변경 (CQRS)

### 2.1 잘 되어 있는 것

**Write(Command) 분해는 방법론이 요구하는 수준을 이미 넘어선다.** SRS가 *"모든 쓰기는 Server Action을 통과한다"*(C-TEC-009)로 런타임을 강제했기 때문에, Command 태스크가 액션 단위로 자연히 1:1 분리되어 있다 — `approveMission`(FR-047) · `submitRetrospective`(FR-065) 등.

### 2.2 수정 여지 ③ — Query가 UI에 묶여 있어 CQRS가 깨진다 🟠

SRS는 **RSC(읽기) / Server Action(쓰기)** 로 런타임을 이미 나눴는데(§3.1), 태스크 분해는 **화면 단위**로 묶여 있어 그 분리가 태스크에 반영되지 않았다. 해당 태스크 **16건**:

```
FR-032 FR-038 FR-039 FR-044 FR-045 FR-050 FR-052 FR-054
FR-056 FR-066 FR-090 FR-092 FR-093 FR-094 FR-101 FR-106
```

가장 뚜렷한 예 — **FR-090 `(guardian)/tree` 성장 나무 화면 — 병렬 4쿼리 · 배치 산출값 읽기 전용**. 한 태스크 안에 **①4개 병렬 쿼리 설계(백엔드 · §8.1 예산 180ms 구간)** 와 **②Accordion·Badge 렌더(프론트 · UX-012·013)** 가 함께 있다. 담당자도, 검증 방식도, 완료 판정도 서로 다르다.

**제안** — 화면 태스크를 2분할한다. 전체를 나눌 필요는 없고, **데이터 조회가 실질적인 6건**만 나누면 충분하다.

| 분할 대상 | `[Feature/Query]` | `[UI]` | 근거 |
| --- | --- | --- | --- |
| FR-090 성장 나무 | 4영역 병렬 조회 · 정체값 읽기 | 나무 렌더 · 정체 원인 표시 | §8.1 예산 구간 6 |
| FR-092 월간 숲 | 스냅샷 조회 · 전월 부재 판정 | Tabs · 대체 문구 | ACE-1.2 |
| FR-093 소비 내역 | 월간 집계 테이블 조회 | 증감액 상단 배치 | §4.1 (FUNC-013) |
| FR-050 승인 대기 | 대기 건수·목록 조회 | Table · Dialog · 일괄 선택 | REQ-FUNC-010 |
| FR-044 학습·퀴즈 | 원고·진도 조회 | RadioGroup · Progress | REQ-FUNC-003 |
| FR-066 회고 | 미완 큐 조회 · 병합 판정 | 갈래별 색 분기 | ACE-5.2 |

> 나머지 10건(FR-032 · 038 · 039 · 045 · 052 · 054 · 056 · 094 · 101 · 106)은 **조회가 얕거나 게이트·셸 성격**이라 분할 이득이 없다. **분할하지 않는 편이 낫다** — 태스크 수만 늘고 병렬화 이득이 없다.

분할 시 FR 총계는 116 → **116 + 6(Query) + 6(Contract) + 3(Mock) = 131건**이 된다.

---

## 3. Step 3 — AC의 테스트 태스크 변환

### 3.1 수정 여지 ④ — AC 매핑이 34%에 그친다 🔴 (가장 큰 결함)

| 지표 | 값 |
| --- | --- |
| 수용 기준 총량 | **84건** (기준 SRS 49 + nextjs SRS 35) |
| 태스크 문서에 인용된 AC | **29건 (34%)** |
| 미인용 | **55건** |
| 기능성 태스크인데 AC 참조가 없는 것 | **56건** |

`approveMission`(FR-047)조차 `관련 SRS 섹션` 칸이 `6.1.1 · REQ-FUNC-002 · 010`이고, **정작 이 태스크를 판정하는 AC-6.1 · ACE-6.2 · AC-T3.1이 적혀 있지 않다.** 템플릿의 `🧪 Acceptance Criteria` 슬롯을 채우려면 매번 두 SRS를 다시 뒤져야 한다.

**제안 — 문서에 「AC ↔ 태스크」 매핑 절을 신설한다.** 이것이 **본문 작성 전 반드시 선행되어야 할 단 하나의 수정**이다. 없으면 131건 각각에서 같은 탐색을 반복하게 된다.

| 신설 절(안) | 내용 |
| --- | --- |
| `§8 수용 기준 배분표` | AC/ACE 49건 + AC-T/AC-A 35건 = 84행. 각 행에 **귀속 태스크 ID**와 **검증 레벨**(단위/통합/E2E/부하/감사) |
| 역방향 확인 | AC가 **하나도 귀속되지 않은 태스크**와 **어느 태스크에도 안 붙은 AC**를 각각 목록화 |

### 3.2 수정 여지 ⑤ — 테스트가 뒤로 몰려 TDD가 성립하지 않는다 🟠

현재 E20(FR-107~116) 10건이 **전 검증을 담당**한다. 그런데 방법론은 *"AC를 에이전트가 실행할 테스트 코드 작성 Task로 변환"* 하고 *"AC 내용은 Feature 이슈의 DoD 체크리스트로 삽입"* 하라고 요구한다. 현재 구조는 **구현이 다 끝난 뒤 검증하는 배치**다.

**제안 — 두 종류를 구분한다.** E20을 없애는 것이 아니라 **성격을 나눈다.**

| 구분 | 어디에 두나 | 해당 |
| --- | --- | --- |
| **기능별 GWT 테스트** | 해당 Feature 태스크의 **DoD 체크리스트 + Task Breakdown 마지막 항목**으로 흡수 | AC-6.1 · ACE-6.2 등 기능 AC |
| **횡단 검증 설비** | E20에 **유지** — 하네스 자체가 별도 산출물 | 위반 주입 7종(FR-107) · 부하 하네스(FR-109) · 2 OS E2E(FR-111) |

> E20을 그대로 두는 것이 맞는 이유 — 게이트 위반 주입·k6 부하 스크립트·2 OS E2E는 **개별 기능에 귀속되지 않는 인프라성 산출물**이다. 방법론의 Step 4(인프라 태스크)에 더 가깝다.

---

## 4. Step 4 — NFR 추출 및 의존성 매핑

### 4.1 잘 되어 있는 것 — 이 문서의 최대 강점

| 항목 | 현황 |
| --- | --- |
| `Depends on` 보유 | **114 / 116** (FR-001 · FR-002만 `None` — 진입점이므로 정상) |
| `Blocks` 역산 | **76건**이 다른 태스크를 막고 있음이 기계적으로 산출됨 |
| 전방 참조 | **0건** — 선행이 항상 자기보다 작은 번호 |
| 임계 경로 | §7에 이미 명시 (`FR-008 → 011 → 012 → 013 → 026 → 040 → 047 → 059 → 065`) |

방법론의 *"핵심 오케스트레이션"* 은 **추가 작업 없이 GitHub Project에 그대로 올릴 수 있다.**

### 4.2 수정 여지 ⑥ — 성능 예산 계측 태스크가 없다 🟠

SRS §8은 p95 상한을 **구간별 예산으로 쪼개 두었다**(나무 1,250ms를 8구간, ⭐ 반영 800ms를 6구간). 그런데 이 예산을 **실제로 재는 태스크가 없다.**

현재 성능 관련 태스크는 4건뿐이고, 그중 **FR-109는 동시성·정합성 중심**이며 **FR-087은 가용성 프로브**다. *"어느 구간에서 예산을 초과했는가"* 를 판정할 수단이 태스크로 존재하지 않는다.

**제안** — 신설 2건 (방법론의 *"[Infra] k6 부하 테스트 스크립트"* 에 대응).

| 신설(안) | 근거 |
| --- | --- |
| `[Infra] 런타임 예산 구간 계측 하네스` — §8.1·8.2의 구간별 소요를 분리 측정 | §8.4 *"1. 어느 구간이 초과했는지 계측으로 특정"* 이 절차로 규정돼 있으나 수단이 없음 |
| `[Infra] 콜드스타트 완화 · 번들 예산 감시` | §8.1 구간 3(250ms · 예산의 22%)이 *"가장 위험한 칸"* 으로 명시됨 |

### 4.3 수정 여지 ⑦ — 보안 태스크가 유형으로 드러나지 않는다 🟡

방법론은 `[Sec]` 태스크를 별도 유형으로 뽑는다. 현재 문서에서 보안·규제는 **여러 Epic에 흩어져 있다** — FR-011(롤 분리) · FR-012(RLS) · FR-014(pii 함수) · FR-019·020(게이트) · FR-032(동의 게이트) · FR-057(HMAC) · FR-103(프롬프트 화이트리스트).

**수정 제안은 태스크 재배치가 아니라 라벨이다.** 이들에 `gate:regulatory` · `type:security` 라벨을 부여하면 GitHub Project에서 **규제 계층 1·2 태스크만 한 번에 필터**할 수 있다. 이미 템플릿 주석에 규약이 있으므로 **배분표에 유형 칸만 추가**하면 된다.

---

## 5. 수정 항목 요약 — 우선순위

| 순위 | 수정 | 대상 | 영향 | 미조치 시 |
| :-: | --- | --- | :-: | --- |
| **1** 🔴 | **§8 AC ↔ 태스크 배분표 신설** (84행) | 문서 전체 | 전역 | 131건 각각에서 SRS 재탐색 반복 |
| **2** 🔴 | **`[Contract]` 태스크 6건 신설** | E4 | 전역 | 프론트가 백엔드 완료를 대기 |
| **3** 🔴 | **`[Mock]` 태스크 3건 신설** | E1 또는 신설 Epic | 전역 | **D-TEC-2 지연 시 B4 전면 정지** |
| **4** 🟠 | **화면 6건을 Query / UI로 2분할** | E7·E8·E16 | 국소 | 담당·검증이 다른 작업이 한 이슈에 혼재 |
| **5** 🟠 | **기능별 GWT를 Feature DoD로 흡수** | E20 성격 재정의 | 국소 | 구현 후 검증 — TDD 불성립 |
| **6** 🟠 | **`[Infra]` 성능 계측 2건 신설** | E18 | 국소 | §8.4 절차의 1단계 수행 불가 |
| **7** 🟡 | **유형 라벨 칸 추가** (`type:` · `gate:`) | 전 표 | 국소 | 규제 태스크 필터링 불가 |

**적용 후 규모** — FR 116 → **133건** · UX 23 유지 · **총 156건**

| 신설·분할 | 건수 | 출처 |
| --- | :-: | --- |
| `[Contract]` 계약 정의 | +6 | 수정 2 |
| `[Mock]` 모킹 | +3 | 수정 3 |
| `[Feature/Query]` 분할 (FR-044·050·066·090·092·093) | +6 | 수정 4 |
| `[Infra]` 성능 계측 | +2 | 수정 6 |
| **소계** | **+17** | 116 → 133 |

---

## 6. 풀버전 작성 순서 — 방법론 4단계를 반영한 재배열

이전에 제안한 순서는 **빌드 순서(B0→B5)** 를 따랐다. 방법론에 맞추려면 **계약이 전 빌드보다 먼저** 나와야 하므로 앞부분을 재배열한다. 원칙은 그대로 둔다 — **`Depends on`을 채우려면 선행 태스크 문서가 먼저 존재해야 한다.**

### Stage 0 — 문서 개정 *(본문 0건)*

> §5의 수정 1~7을 태스크 리스트 문서에 반영한다. **여기서 AC 배분표(수정 1)를 만들지 않으면 이후 전 단계가 느려진다.**

### Stage 1 — 계약·데이터 *(Step 1)* · 19건

| 순서 | 대상 | 건수 | 이유 |
| :-: | --- | :-: | --- |
| 1-A | **E2 DB Schema & Access** (FR-008~017) | 10 | 스키마가 모든 계약의 타입 원천 |
| 1-B | **`[Contract]` 신설 6건** | 6 | 프론트·백 병렬의 기준점 |
| 1-C | **`[Mock]` 신설 3건** | 3 | 1-B 계약이 있어야 Mock을 만들 수 있다 |

### Stage 2 — 실행 규약·강제 설비 · 27건

| 순서 | 대상 | 건수 | 이유 |
| :-: | --- | :-: | --- |
| 2-A | **E4 Server Runtime & Domain** (FR-026~029) | 4 | ★ **체크포인트** — 이후 60여 건이 이 문장을 재사용 |
| 2-B | **E3 Build Gate** (FR-018~025) | 8 | 첫 커밋부터 켜져 있어야 위반이 안 쌓인다 |
| 2-C | **E1 Platform** (FR-001~007) + **E12 Batch Infra** (FR-067~069) | 10 | 설정 계열 — 병렬 작성 가능 |
| 2-D | **UX-001~005 Design System** | 5 | FR-007(Tailwind·shadcn 초기화)과 짝. 이후 전 화면 태스크가 참조 |

### Stage 3~7 — 빌드별 Command → Query → Test *(Step 2·3)*

> 각 Stage 안에서 **① Command(쓰기) → ② Query(조회) → ③ UI → ④ 해당 GWT를 DoD로 확정** 순으로 작성한다. Command 문서가 먼저 있어야 Query·UI가 참조할 계약이 확정된다.

| Stage | 빌드 | 대상 | 건수 |
| :-: | :-: | --- | :-: |
| **3** | B1 | E5 Auth + E6 Onboarding + UX-006~009 | 14 |
| **4** | B2 | E7 Star Ledger & Learning (+Query 분할 1) + UX-010·011 | 9 |
| **5** | B3 | E8 Mission (+Query 1) + E9 Wishlist/Avatar + E17 PWA/Offline + UX 4 | 18 |
| **6** | B4 | E10 Partner & Spending + UX-017 | 10 |
| **7** | B5 | E11 Retro (+Query 1) + E13 pg_cron + E14 Notification + E15 Vercel Cron + UX-018 | 25 |

### Stage 8 — 전달 화면 *(Query/UI 분할 적용)* · 12건

E16 Growth Delivery (FR-090·092·093의 Query 3 + UI 3 + FR-091) + UX-012·013·014·019·022

> **이 Stage가 Mock의 효용이 드러나는 지점이다.** Stage 1-C의 배치 산출값 픽스처가 있으면 **Stage 7(pg_cron)과 병렬로** 작성·구현할 수 있다.

### Stage 9 — 비기능·인프라 *(Step 4)* · 12건

E18 Observability(3) + **`[Infra]` 성능 계측 신설 2**(수정 6) + E19 AI Ops(6) + UX-023(1)

### Stage 10 — 횡단 검증 설비 · 10건

E20 Verification Harness (FR-107~116) — 기능별 GWT는 Stage 3~8에서 각 DoD로 이미 흡수됨

### Stage 11 — 의존성 확정 및 이슈화 *(Step 4 오케스트레이션)* · 본문 0건

1. 154건 전수의 `Depends on` / `Blocks` 재검증 (분할·신설분 반영)
2. `gh issue create -F` 로 일괄 생성
3. `tasks/_index.md` 매핑으로 `FR-###` → `#번호` 치환
4. 마일스톤(B0~B5·조건부·검증) · 라벨(`type:` · `epic:` · `complexity:` · `gate:`) 부여

### 전체 요약

| Stage | 성격 | FR | UX | 계 |
| :-: | --- | :-: | :-: | :-: |
| 0 | 문서 개정 | 0 | 0 | **0** |
| 1 | 계약·데이터 (Step 1) | 19 | 0 | **19** |
| 2 | 실행 규약·강제 설비 | 22 | 5 | **27** |
| 3 | B1 동의·계정 | 10 | 4 | **14** |
| 4 | B2 별·학습 | 7 | 2 | **9** |
| 5 | B3 실천·나무·PWA | 14 | 4 | **18** |
| 6 | B4 소비 | 9 | 1 | **10** |
| 7 | B5 회고·배치·알림 | 24 | 1 | **25** |
| 8 | 전달 화면 (Query/UI 분할) | 7 | 5 | **12** |
| 9 | 비기능·인프라 (Step 4) | 11 | 1 | **12** |
| 10 | 횡단 검증 설비 | 10 | 0 | **10** |
| 11 | 의존성 확정·이슈화 | 0 | 0 | **0** |
| | **합계** | **133** | **23** | **156** |

---

## 7. 방법론과 이 프로젝트의 구조적 차이 — 조정하지 말아야 할 것 2가지

평가 중 드러난 것으로, **방법론을 그대로 적용하면 오히려 어긋나는 지점**이다.

| # | 방법론 | 이 프로젝트 | 판단 |
| :-: | --- | --- | --- |
| **1** | *"[API Spec] … DTO 및 에러 코드"* | **공개 REST API가 없다**(§6.1). Server Action은 동일 출처 POST이며 HTTP 상태 코드로 판정하지 않는다 | 계약 태스크는 만들되 **산출물을 `zod` 스키마 + 반환 타입 + 실패 분기**로 정의. `409 Conflict` 같은 HTTP 코드 규약은 **Route Handler 9종에만** 적용 |
| **2** | *"NFR을 인프라·DevOps Task로 추출"* | SRS §13.1이 *"비기능에 별도 sp를 부여하지 않는다 — 분리해 배분하면 규제·정합성 항목이 뒤로 밀린다"* 로 **명시적으로 금지** | **규제·정합성 NFR**(REQ-NF-006·007·008·009·010)은 기능 태스크의 완료 조건에 유지. **성능·운영 NFR**만 `[Infra]` 태스크로 분리 — 이것이 §5 수정 6번의 범위인 이유 |

> 특히 2번은 중요하다. 방법론대로 규제 NFR까지 별도 인프라 태스크로 빼면 **SRS가 명시적으로 막아 둔 실패 모드**(규제 항목이 일정에서 뒤로 밀림)로 들어간다. 방법론의 취지(NFR을 잊지 않기)는 **DoD 체크리스트 흡수**로 달성하는 편이 이 프로젝트에 맞다.

---

*평가 기준: 사용자 제시 4단계 태스크 추출 방법론 · 대조 대상: `TASKS_finfriends-nextjs-v1_0.md` (2026-08-24 · 139건)*
