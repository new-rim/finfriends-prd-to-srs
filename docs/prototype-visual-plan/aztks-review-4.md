# AZTKS 평가 — 라운드 4 (구현물)

**일시:** 2026-08-27 · **커밋:** `ede9181` 시점
**통과선:** 🔴 `VERDICT: GO` **且** 5축 전부 `P` (CONCERN 0건)

> ⚠️ 첫 시도는 API 주간 한도로 결과 없이 중단됐다. 같은 디스패치를 다시 걸어 받은 결과이며,
> 중단분은 라운드 카운트에 넣지 않았다.

## 반환 원문

```
VERDICT: NO-GO *(디스패치 상향 통과선 — CONCERN 2건)*
SCORECARD: A:P Z:C T:P K:P S:C

TOP_FIX: 테마 변수를 **소비 지점과 같은 스코프**로 올려라 — `app/globals.css:51-58`의
`html, body { background/color/font-size/line-height }`를 `[data-theme] { … + min-height:100dvh }`로
옮기고, `scripts/proto/verify.mjs` L4에 「globals.css가 참조하는 `var(--x)`가 정의 스코프 안에서
소비되는가」 검사 1건을 추가한 뒤 스냅샷을 재생성한다.

EVIDENCE:
- `app/globals.css:51-58` ↔ `:15-46` · `app/(child)/layout.tsx:7` — body가 참조하는
  `--bg·--text-body·--text-line`은 **`[data-theme]` 자손 스코프에만** 정의돼 body에서
  해석되지 않는다. 아동 배경 `#fffdf7` · 본문 18px · 행간 1.7이 **실제 화면에 안 걸린다**.
  `--space-unit`·`--ratio-scale`은 참조 0건 — 계획 §6.1의 **3축 중 크기·타이포 두 축이
  미구현**이고 L4(`verify.mjs:86-90`)는 값만 읽어 이를 못 잡는다.
- `docs/prototype-visual-plan.md:248` 「모아요 · 실천 1회 남음」 ↔ `render-snapshot/tree.txt:35-37`
  「새싹 / 14일째 그대로예요」 — 3R가 화면을 바꾸고 **확정 출처를 안 고쳤다**(런 파일 §6은
  반대 방향). §14.1은 맞췄으나 §5.1이 남았다.
- `render-snapshot/retro.txt:41-42` 「1일치를 모았어요」 ↔ 계획 `:462-464`(예시 "3일치") ·
  `docs/prototype-lite-scope.md:77`(「요약 회고와 단건의 구별」이 판정 대상) — 묶인 게 1건이라
  그 구별을 판정할 수 없다.

NOTES:
- **A·T는 통과** — 두 선언 위계가 `app/page.tsx:10-14`, ②→①(`quiz.tsx:48-50`) ·
  ③→①(`retro/page.tsx:41-43`) · ①의 "3번 적어서"(`narrative.ts:27`)로 닫힌다.
  별 3 = 실천 3이 테스트로 강제된다(5/5 통과 · check-style 0건 · tsc 0).
- **S** — 계획 §5.1과 화면이 갈린 채라 다음 소비자가 문서를 읽으면 정체 칸을 다시 다르게 만든다.
```

## 이 라운드에 반영한 것

| # | 무엇 | 근거 |
| :-: | --- | --- |
| **1** | **TOP_FIX** — 배경·타이포를 `[data-theme]` 자신에게 건다. body에 걸면 `var(--bg)`가 **해석되지 않는다**(CSS 변수는 조상→자손으로만 상속되는데 `[data-theme]`은 body의 자손) | 4R EVIDENCE 1 |
| **2** | `--space-unit`·`--ratio-scale`을 **실제로 소비** — Card·Alert 패딩, 제목 크기. 3축 중 두 축이 값만 있고 화면에 안 걸려 있었다 | 계획 §6.1 |
| **3** | **L4에 검사 2건 추가** — ① 정의 스코프 밖에서 참조하는 테마 변수 ② 참조 0건인 테마 변수. 종전 L4는 값만 읽어 둘 다 못 잡았다 | 재발 방지 |
| **4** | 모아요 칸에 **「실천 1회 남음」 복원** · 「14일째 그대로예요」 제거 — 도면(§5.1)대로 | 런 파일 §4·§6 |
| **5** | `?state=backlog` 추가 — 요약 회고에 **2건 이상** 묶인 상태를 스냅샷으로 뜬다 | 4R EVIDENCE 3 |

## 🔴 4번을 문서가 아니라 화면에서 고친 이유

4R EVIDENCE 2는 **문서와 화면이 갈렸다**고만 지목했다. 어느 쪽을 고칠지는 런 파일이 정한다 —
§4가 `docs/prototype-visual-plan.md` **수정을 금지**하고, §6이 *"문서를 고치지 말고 화면을 고친다"*
고 못 박았다. 그래서 **화면을 도면에 맞췄다.**

다만 3R가 지적한 중복(카드 3줄 ↔ 정체 블록 3줄)이 되살아나지 않게, **실천이 0인 칸에서만
「가장 적게 남은 조건」 하나**를 적는다. 도면이 원래 그 형태였고(`실천 1회 남음` 한 줄),
실천이 있는 칸은 2R TOP_FIX대로 남은 학습·퀴즈를 함께 적는다.

**5번도 기본 시나리오를 건드리지 않았다** — §14.1이 회고 4건으로 확정했으므로, 요약 구별을
보여줄 상태는 `RETRO_BACKLOG` **변형**으로 따로 만들었다. 빈 상태를 `?state=empty`로 뜬 것과
같은 방식이다.
