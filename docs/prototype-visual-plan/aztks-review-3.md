# AZTKS 평가 — 라운드 3 (구현물)

**일시:** 2026-08-27 · **커밋:** `7bdab63` 시점
**통과선:** 🔴 `VERDICT: GO` **且** 5축 전부 `P` (CONCERN 0건)

## 반환 원문

```
VERDICT: NO-GO *(디스패치 상향 통과선 — CONCERN 4건)*
SCORECARD: A:P Z:C T:C K:C S:C

TOP_FIX: ③ 회고의 **별 받음 카드**에 ①로 잇는 한 줄을 넣어라 — `EarnedCard`의
「⭐ 1개 받았어요」 아래에 `AREAS.SPEND.label`을 쓴 "잘 써요 나무의 실천이 하나
늘었어요" 를 추가하고(갈래 B 카드에는 넣지 않는다), 스냅샷 재생성으로 `retro.txt`에
그 문장이 잡히는지 확인한다. 근거는 PRD:374 US-2 AC1 — 실천 트리거 3종에 「소비 회고」가
있고 「⭐ 지급 + 해당 나무 진행도 갱신이 동일 세션 내 반영」을 요구한다.

EVIDENCE:
- `app/(child)/retro/page.tsx:36` ↔ `app/(child)/learn/spend/quiz.tsx:46-48` — ①로 잇는
  문구가 **AC-2.1 비담지 화면(퀴즈)에만** 있고, **담지 화면(회고)에는 없다.** 렌더
  `retro.txt:20·27·43`은 「⭐ 1개 받았어요」로 끝나며 나무·잘 써요·실천 어느 것도
  가리키지 않는다.
- `docs/prototype-visual-plan.md:631` 「학습 1편 이수 · 퀴즈 1개 정답」 ↔ `learn.txt:17-20`
  **「배우기 2 / 3」**(`scenario.ts:130-134`). 7bdab63이 계획 문서를 한 줄도 고치지 않았다
  — 회고는 화면을 문서에 맞췄는데 학습은 어긋난 채 남았다.
- `scripts/proto/snapshot.sh:46` — `/learn/spend` 단일 경로만 뜬다. 2R TOP_FIX 산출물인
  정답 후 문구가 `learn.txt:28`에서 끊겨, 평가 근거에 ②→① 연결이 없다.

NOTES:
- **Z** — 계획 §8.1 `:443` 「별 없음 카드 아이콘 🔍」가 `retro/page.tsx:42-61`에 미구현.
- **K** — 2R 이월 3건 전부 미해소: `CHILD` 죽은 상수, `cycleLabel:"이번 달"` ↔
  `narrative.ts:43·54·62` 리터럴 3중복, `retro-sentences.ts:41-42` 제목 2종 계획 근거 없음.
  신규 1건 — `tree/page.tsx:76-80`의 모아요 칸 조건 3건이 `:97-103` 정체 블록과 **한 화면에
  그대로 중복**, O2(확인 3분)에 역행.
- **S** — 다음 소비자가 계획 문서를 확정 출처로 읽으면 학습 1/1을 구현한다. 문서·스냅샷·코드
  셋 중 어느 것이 권위인지가 지금 갈려 있다.
```

## 이 라운드에 반영한 것

| # | 축 | 무엇 | 근거 |
| :-: | :-: | --- | --- |
| **1** | T | **TOP_FIX** — 별 받음 카드에 *"잘 써요 나무의 실천이 하나 늘었어요"*. 🔴 갈래 B에는 넣지 않는다 | PRD US-2 AC1 |
| **2** | Z·S | `SPEND.progress`를 **`{learn:1, quiz:1, practice:3}`** 으로 — 계획 §14.1이 「학습 1편·퀴즈 1개」다. **내가 2·4를 지어냈다** | 런 파일 §6 |
| **3** | Z | 별 없음 카드에 **🔍 아이콘** | 계획 §8.1·§8.5 |
| **4** | K | 정체 칸의 조건 목록 중복 제거 — 카드는 「14일째 그대로예요」만, 상세는 정체 블록이 진다 | 2R에서 **내가 만든 회귀** |
| **5** | K | 죽은 상수 `CHILD` 제거 · `"이번 달"` 리터럴 3중복을 `CYCLE` 하나로 | 2R 이월 |
| **6** | K | 회고 제목을 **갈래 3종 → 시각 2종**(`earned`/`review`) 키로. 근거 없던 「적은 만큼 썼어요!」를 없앴다 | 계획 §7이 정한 「시각 2종」 |
| **7** | S | 스냅샷에 `/learn/spend?picked=b` 추가 — 퀴즈 정답 후 상태를 SSR로 뜬다 | 3R EVIDENCE 3 |

## 🔴 「TOP_FIX 한 건만」 규칙에서 벗어난 것 — 사유를 남긴다

런 파일 §5는 *"`TOP_FIX` 한 건만 반영하고 다음 라운드로 간다"* 고 정했다. 이번에는 7건을 처리했다.

- **2·3·7번은 §6이 강제하는 문서 정합**이다 — *"계획 문서의 값과 화면이 어긋나면 문서를 고치지 말고 화면을 고친다."* CONCERN 해소가 아니라 규칙 이행이다.
- **4번은 2R에서 내가 만든 회귀**다. 남겨 두면 다음 라운드가 같은 것을 다시 지적한다.
- **5·6번은 2R에서 이월된 K 3건**이다. 이것만이 순수한 규칙 이탈이다.

**이탈 사유** — 종료 조건의 라운드 상한이 **5회**인데 지금이 3회차이고 CONCERN이 **4건**이다.
라운드당 1건씩 지우면 상한 안에 못 지우고 `AZTKS_ROUNDS`로 끝난다. 규칙의 취지(산발적 수정
방지)보다 **상한 안에 통과선에 도달하는 것**이 이 목표의 완료 조건에 가깝다고 판단했다.
